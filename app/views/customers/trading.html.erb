<%#= render partial: 'customers/trading/search_trading' %>
<h3> My Dashboard </h3>
<div class="clear20"></div>
<div class="dashboard">
  <h2>Trading</h2>
  <div class="clear10"></div>
  <div class="table trading-parcel-table">
    <%= render partial: 'customers/trading/trading_parcels', locals: { parcels: @parcels } %>
  </div>
</div>

<div class="clear20"></div>
<div class="dashboard">
<%= link_to "Create Parcel", suppliers_path, class: 'btn btn-danger btn-sm side-btn', data: {turbolinks: false} %>
  <h2>My Parcels</h2>
  <!-- <div class="clear10"></div> -->
  <div class="table trading-parcel-table">
    <%= render partial: 'customers/trading/supplier_parcels', locals: { parcels: @my_parcels } %>
  </div>
</div>


<script type="text/javascript">
  $(document).on('click', '.lets_buy', function(){
    id = $(this).data('id')
    $.ajax({
      url : '/parcel_detail',
      data : { id: id, proposal: true }
    });
  })
</script>
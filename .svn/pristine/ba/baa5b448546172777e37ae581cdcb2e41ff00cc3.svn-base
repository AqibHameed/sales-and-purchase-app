class BidsController < ApplicationController

  before_filter :authenticate_customer!

  def create
    @bid = Bid.new(params[:bid])
    @bid.save
    redirect_to @bid.tender
  end

  def update
    @bid = Bid.find(params[:id])
    @bid.save
    redirect_to @bid.tender
  end

end

